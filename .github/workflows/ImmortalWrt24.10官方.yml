name: 官方分支编译 (ImmortalWrt 24.10)

on:
  repository_dispatch:
  workflow_dispatch:

env:
  # 这里已经换成了最纯正的 ImmortalWrt 官方仓库和 24.10 主线分支
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  REPO_BRANCH: openwrt-24.10
  NEW_LAN_IP: 192.168.6.1
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    if: github.event.repository.owner.id == github.event.sender.id

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: 初始化官方编译环境
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf /usr/share/dotnet /etc/apt/sources.list.d /usr/local/lib/android 2>/dev/null
          sudo -E apt-get -y update
          sudo -E apt-get -y purge azure-cli ghc* zulu* llvm* firefox google* dotnet* powershell openjdk* mongodb* moby* || true
          # 👇 就在下面这行！彻底抛弃失效短链，换成官方直链，绝对秒过！
          sudo -E apt-get -y install $(curl -fsSL https://raw.githubusercontent.com/P3TERX/Actions-OpenWrt/main/depends-ubuntu-2204)
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get clean
          sudo timedatectl set-timezone "${TZ}"

      - name: 拉取官方源码
        run: |
          git clone -q --single-branch --depth=1 --branch=${REPO_BRANCH} ${REPO_URL} openwrt

      - name: 更新并安装 Feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 运行 diy2 自定义脚本
        run: |
          cd openwrt
          # 确保脚本有执行权限并运行（即使脚本不存在也不会中断流程）
          [ -f ../scripts/diy2.sh ] && chmod +x ../scripts/diy2.sh && bash ../scripts/diy2.sh || echo "未找到 diy2.sh，跳过"

      - name: 修改默认 LAN IP
        run: |
          cd openwrt
          sed -i "s/192.168.6.1/${NEW_LAN_IP}/g" package/base-files/files/bin/config_generate

      - name: 加载纯净版 .config
        run: |
          [ -e .config ] && mv .config openwrt/.config

      - name: 集成自定义 files 文件夹 (如果存在)
        run: |
          if [ -d "files" ]; then
            cp -a files openwrt/files
            echo "自定义 files 文件夹已成功注入。"
          else
            echo "未检测到 files 文件夹，跳过注入。"
          fi

      - name: 下载依赖包
        run: |
          cd openwrt
          make defconfig
          make download -j8
          find dl -size -1024c -exec rm -f {} \;

      - name: 开启官方标准编译
        id: compile
        run: |
          cd openwrt
          echo -e "$(nproc) thread compile"
          make -j$(($(nproc) + 1)) V=s || make -j1 || make -j1 V=s
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: 整理打包出的固件
        id: organize
        if: steps.compile.outputs.status == 'success' && !cancelled()
        run: |
          cd openwrt/bin/targets/*/*
          rm -rf packages
          echo "FIRMWARE=$PWD" >> $GITHUB_ENV
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: 上传固件到 Artifacts
        uses: actions/upload-artifact@main
        if: steps.organize.outputs.status == 'success' && !cancelled()
        with:
          name: RAX3000M_Official_Firmware
          path: ${{ env.FIRMWARE }}
